apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: servease
  labels:
    app: appointment-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
    spec:
      containers:
      - name: appointment-service
        image: servease/appointment-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8005
          name: http
        env:
        - name: SERVICE_NAME
          value: "appointment-service"
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: DB_HOST
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: APPOINTMENT_DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: DB_PASSWORD
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: REDIS_HOST
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: AUTH_SERVICE_URL
        - name: CUSTOMER_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: CUSTOMER_SERVICE_URL
        - name: EMPLOYEE_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: EMPLOYEE_SERVICE_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: servease
spec:
  selector:
    app: appointment-service
  ports:
  - port: 8005
    targetPort: 8005
    protocol: TCP
  type: ClusterIP
