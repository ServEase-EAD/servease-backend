apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: servease
  labels:
    app: admin-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
      - name: admin-service
        image: servease/admin-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8007
          name: http
        env:
        - name: SERVICE_NAME
          value: "admin-service"
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: DB_HOST
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: ADMIN_DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: DB_PASSWORD
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: REDIS_HOST
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: AUTH_SERVICE_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 8007
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8007
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: servease
spec:
  selector:
    app: admin-service
  ports:
  - port: 8007
    targetPort: 8007
    protocol: TCP
  type: ClusterIP
