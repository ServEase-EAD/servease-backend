apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentication-service
  namespace: servease
  labels:
    app: authentication-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: authentication-service
  template:
    metadata:
      labels:
        app: authentication-service
    spec:
      containers:
      - name: authentication-service
        image: servease/authentication-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8001
          name: http
        env:
        - name: SERVICE_NAME
          value: "authentication-service"
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: DB_HOST
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: AUTH_DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: DB_PASSWORD
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: REDIS_HOST
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: authentication-service
  namespace: servease
spec:
  selector:
    app: authentication-service
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
  type: ClusterIP
