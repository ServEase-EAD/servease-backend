apiVersion: apps/v1
kind: Deployment
metadata:
  name: vehicleandproject-service
  namespace: servease
  labels:
    app: vehicleandproject-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vehicleandproject-service
  template:
    metadata:
      labels:
        app: vehicleandproject-service
    spec:
      containers:
      - name: vehicleandproject-service
        image: servease/vehicleandproject-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8004
          name: http
        env:
        - name: SERVICE_NAME
          value: "vehicleandproject-service"
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: DB_HOST
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: VEHICLE_DB_NAME
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: servease-secrets
              key: DB_PASSWORD
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: REDIS_HOST
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: servease-config
              key: AUTH_SERVICE_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: vehicleandproject-service
  namespace: servease
spec:
  selector:
    app: vehicleandproject-service
  ports:
  - port: 8004
    targetPort: 8004
    protocol: TCP
  type: ClusterIP
